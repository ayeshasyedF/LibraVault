def borrow_book(user_id, book_id):

    return f"Borrow book {book_id} for user {user_id}"


def return_book(book_id):

    return f"Return book {book_id}"


def reserve_book(user_id, book_id):

    return f"Reserve book {book_id} for user {user_id}"